import { ComponentStyleConfig } from '@chakra-ui/react'

export const Pokemon: ComponentStyleConfig = {
  parts: ['wrapper'],
  baseStyle: {
    wrapper: {
      backgroundColor: 'brand.900',
      color: 'white',
    },
  },
}
