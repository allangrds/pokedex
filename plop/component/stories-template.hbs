import * as React from 'react'
import { ComponentStory, ComponentMeta } from '@storybook/react'

import { {{pascalCase component_name}} } from './{{kebabCase component_name}}'

export default {
  title: 'Components/{{pascalCase component_name}}',
  component: {{pascalCase component_name}},
  argTypes: {
    backgroundColor: { control: 'color' },
  },
} as ComponentMeta<typeof {{pascalCase component_name}}>

const Template: ComponentStory<typeof {{pascalCase component_name}}> = (args: any) => (
  <{{pascalCase component_name}} {...args} />
)

export const Primary = Template.bind({})
Primary.args = {
  primary: true,
  label: 'Button',
}
